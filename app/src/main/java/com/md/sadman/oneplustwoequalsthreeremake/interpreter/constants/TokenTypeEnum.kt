package com.md.sadman.oneplustwoequalsthreeremake.interpreter.constants

/**
 * Token Type Enum
 *
 * @author Md. Sadman
 */
enum class TokenTypeEnum(val value: Int) {
    NUMBER(0),
    PLUS(1),
    MINUS(2),
    MULTIPLY(3),
    DIVIDE(4)
}